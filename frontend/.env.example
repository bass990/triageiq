# Copy this file to .env and adjust the API URL for your local dev server.
# The real .env is gitignored, this template is committed.

# Where the React frontend should send API requests.
# Default is the FastAPI backend running on the same machine.
VITE_API_URL=http://localhost:8000
